# 数据来源说明

## 1. 配方参数：来自用户PPT
G1–G6 的 SA、CS、戊二醛和添加剂参数已写入 `data/ppt_formula_seed.csv`。

## 2. 环境参数：运行时网页API
- NASA POWER：按经纬度/日期抓取每日气象和太阳辐射。
- SoilGrids：按经纬度抓取0–30 cm背景土壤性质。仅作先验，不替代田间采样。

## 3. 田间结果
用户PPT已经有靖安番茄水分、养分、病害等结果，但不同页面存在组号/指标口径需要统一的情况。
因此本原型没有把这些汇总数值强行拼成“训练行”，而是要求按独立小区/重复回填 `data/real_trials_template.csv`。

## 4. 演示数据
`demo_data.py` 的所有结果字段均为合成数据，仅用于证明代码链路可运行，不能作为论文、比赛或产品配方依据。
